name: Update IPTV Source

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *'

jobs:
  update:
    permissions:
      contents: write

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download IPTV sources
        run: |
          mkdir -p output

          curl -L https://iptv-org.github.io/iptv/countries/cn.m3u -o iptvorg.m3u

          curl -L https://raw.githubusercontent.com/YueChan/Live/main/IPTV.m3u -o yuechan.m3u

          curl -L https://raw.githubusercontent.com/suxuang/myIPTV/main/ipv4.m3u -o suxuang.m3u
          
          curl -L https://raw.githubusercontent.com/Kimentanm/aptv/master/m3u/iptv.m3u -o kimentanm.m3u

          cat iptvorg.m3u yuechan.m3u suxuang.m3u kimentanm.m3u > output/source.m3u


      - name: Commit update
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          git commit -m "Update IPTV source" || exit 0
          git push
